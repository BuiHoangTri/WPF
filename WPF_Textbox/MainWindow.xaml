<Window x:Class="WPF_Textbox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Textbox"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <TextBox x:Name="txbValue" 
                 FontSize="20"
                 VerticalAlignment="Center"
                 TextChanged="txbValue_TextChanged">
            
        </TextBox>
        <TextBlock x:Name="txblValue" 
                   VerticalAlignment="Top">
            
        </TextBlock>
        <Button x:Name="btnValue" 
                Content="Click" 
                VerticalAlignment="Bottom"
                Click="btnValue_Click">
            
        </Button>

        <!--<PasswordBox Password="www" PasswordChar="x">
            
        </PasswordBox>-->
    </Grid>
</Window>
